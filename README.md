# MediBot – Medical Chatbot

This project is a starter structure for a medical chatbot built with Python, LangChain, Pinecone, and Flask.

## Structure

- `src/` – Python package with helper code and prompts
- `research/` – Jupyter notebooks for experiments
- `app.py` – Application entry point
- `requirements.txt` – Python dependencies

